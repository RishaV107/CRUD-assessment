class Student {
  constructor(name, className, rollNumber, section) {
    this.name = name;
    this.className = className;
    this.rollNumber = rollNumber;
    this.section = section;
  }
}

export default Student;
